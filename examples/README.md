To run an example:

```
export OPENAI_API_KEY="<your key here>"
go run ./examples/<target>
```

The [Responses API example](responses/README.md) demonstrates tools, structured
output, response chaining, streaming, background mode, token counting, and
compaction.
